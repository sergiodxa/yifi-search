import React from 'React';
import { Link } from 'react-router';

export default React.createClass({
  render() {
    return (
      <header>
        <h1>YIFI Search</h1>
      </header>
    );
  }
});
